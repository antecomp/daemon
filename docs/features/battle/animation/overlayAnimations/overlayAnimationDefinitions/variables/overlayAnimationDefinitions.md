[**daemon**](../../../../../../README.md)

***

# Variable: overlayAnimationDefinitions

> `const` **overlayAnimationDefinitions**: `object`

Defined in: [src/features/battle/animation/overlayAnimations/overlayAnimationDefinitions.ts:14](https://github.com/antecomp/daemon/blob/47daeacebcabea5a8994386f75146796a04a9331/src/features/battle/animation/overlayAnimations/overlayAnimationDefinitions.ts#L14)

Named overlay animations for battle that contain the `src` of the video, and a `width` and `height` for properly scaling the video element.

## Type Declaration

### mirror

> `readonly` **mirror**: `object`

#### mirror.height

> `readonly` **height**: `500` = `500`

#### mirror.src

> `readonly` **src**: `string` = `mirror`

#### mirror.width

> `readonly` **width**: `500` = `500`

### observe

> `readonly` **observe**: `object`

#### observe.blendMode

> `readonly` **blendMode**: `"normal"` = `'normal'`

#### observe.height

> `readonly` **height**: `312` = `312`

#### observe.src

> `readonly` **src**: `string` = `observe`

#### observe.width

> `readonly` **width**: `618` = `618`

### shield

> `readonly` **shield**: `object`

#### shield.height

> `readonly` **height**: `344` = `344`

#### shield.src

> `readonly` **src**: `string` = `shield`

#### shield.width

> `readonly` **width**: `317` = `317`

### slash\_elag

> `readonly` **slash\_elag**: `object`

#### slash\_elag.height

> `readonly` **height**: `625` = `625`

#### slash\_elag.src

> `readonly` **src**: `string` = `slash_elag`

#### slash\_elag.width

> `readonly` **width**: `594` = `594`

### slash\_majes

> `readonly` **slash\_majes**: `object`

#### slash\_majes.height

> `readonly` **height**: `651` = `651`

#### slash\_majes.src

> `readonly` **src**: `string` = `slash_mages`

#### slash\_majes.width

> `readonly` **width**: `812` = `812`

### slash\_norm

> `readonly` **slash\_norm**: `object`

#### slash\_norm.height

> `readonly` **height**: `593` = `593`

#### slash\_norm.src

> `readonly` **src**: `string` = `slashnorm`

#### slash\_norm.width

> `readonly` **width**: `567` = `567`

### slash\_purpose

> `readonly` **slash\_purpose**: `object`

#### slash\_purpose.height

> `readonly` **height**: `610` = `610`

#### slash\_purpose.src

> `readonly` **src**: `string` = `slash_purpose`

#### slash\_purpose.width

> `readonly` **width**: `739` = `739`

### slash\_repeat

> `readonly` **slash\_repeat**: `object`

#### slash\_repeat.height

> `readonly` **height**: `417` = `417`

#### slash\_repeat.src

> `readonly` **src**: `string` = `slash_repeat`

#### slash\_repeat.width

> `readonly` **width**: `447` = `447`
