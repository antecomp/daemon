import { ContextMenu } from "./scenemenu.types";

export default function SceneMenuContainer(props: {
    currentMenu: ContextMenu,
}) {
    return (
        <div id="scenemenu-container">
            {/* todo */}
        </div>
    )
}